syntax = "proto3";

package greet;

option java_package = "com.proto.greet";
option java_multiple_files = true;

message Greeting {
    string first_name = 1;
    string last_name = 2;
}

service GreetService {
    // Unary

    // Server streaming

    // Client Streaming

    // bidi streaming
}